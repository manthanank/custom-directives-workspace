import { CustomSwitchDirective } from './custom-switch.directive';

describe('CustomSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
