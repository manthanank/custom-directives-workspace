import { CustomModelDirective } from './custom-model.directive';

describe('CustomModelDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomModelDirective();
    expect(directive).toBeTruthy();
  });
});
